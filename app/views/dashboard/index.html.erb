<h1>Dashboard#index</h1>
<p>こんにちは、<%= Current.session.user.name %></p>
<% if Current.session.user.admin %>
  <p>管理者</p>
<% end %>

<%= link_to "ログアウト", session_path, data: {turbo_method: :delete, turbo_confirm: 'Are you sure?'} %>
